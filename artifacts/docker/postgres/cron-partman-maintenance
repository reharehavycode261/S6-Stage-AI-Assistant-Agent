# ===============================================
# Cron job pour la maintenance pg_partman
# ===============================================
# Description: Exécute la maintenance automatique chaque jour à 2h du matin
# Format: minute heure jour mois jour_semaine commande
# ===============================================

# Maintenance quotidienne à 2h du matin
0 2 * * * /maintenance-partman.sh >> /var/log/postgresql/partman-maintenance.log 2>&1

# Optionnel: Vérification hebdomadaire le dimanche à 3h du matin
0 3 * * 0 psql -U admin -d ai_agent_admin -c "SELECT partman.check_default();" >> /var/log/postgresql/partman-check.log 2>&1

